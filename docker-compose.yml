version: '3'

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: aluno
      POSTGRES_PASSWORD: aluno
      POSTGRES_DB: gatolandia
    ports:
      - 5432:5432